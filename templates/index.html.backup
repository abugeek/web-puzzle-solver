<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>📱 Universal Phone Puzzle Solver</title>
    <style>
        /* ==================== CSS VARIABLES (Design System) ==================== */
        :root {
            /* Primary Colors */
            --primary-500: #6366f1;
            --primary-600: #4f46e5;
            --primary-700: #4338ca;
            --primary-gradient: linear-gradient(135deg, #6366f1 0%, #8b5cf6 50%, #a855f7 100%);

            /* Neutral Colors */
            --gray-50: #f9fafb;
            --gray-100: #f3f4f6;
            --gray-200: #e5e7eb;
            --gray-300: #d1d5db;
            --gray-400: #9ca3af;
            --gray-500: #6b7280;
            --gray-600: #4b5563;
            --gray-700: #374151;
            --gray-800: #1f2937;
            --gray-900: #111827;

            /* Semantic Colors */
            --success: #10b981;
            --success-light: #d1fae5;
            --warning: #f59e0b;
            --warning-light: #fef3c7;
            --danger: #ef4444;
            --danger-light: #fee2e2;
            --info: #3b82f6;
            --info-light: #dbeafe;

            /* Spacing */
            --space-xs: 0.25rem;
            --space-sm: 0.5rem;
            --space-md: 1rem;
            --space-lg: 1.5rem;
            --space-xl: 2rem;
            --space-2xl: 3rem;

            /* Border Radius */
            --radius-sm: 0.375rem;
            --radius-md: 0.5rem;
            --radius-lg: 0.75rem;
            --radius-xl: 1rem;
            --radius-2xl: 1.5rem;

            /* Shadows */
            --shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
            --shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
            --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
            --shadow-xl: 0 20px 25px -5px rgba(0, 0, 0, 0.1);

            /* Typography */
            --font-sans: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            --font-mono: 'JetBrains Mono', 'Courier New', monospace;

            /* Transitions */
            --transition-fast: 150ms ease;
            --transition-normal: 300ms ease;
        }

        /* ==================== GLOBAL RESET ==================== */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: var(--font-sans);
            background: var(--primary-gradient);
            min-height: 100vh;
            padding: var(--space-lg);
            color: var(--gray-900);
            line-height: 1.6;
        }

        @media (max-width: 640px) {
            body {
                padding: var(--space-md);
            }
        }

        .container {
            max-width: 1400px;
            margin: 0 auto;
        }

        /* ==================== HEADER ==================== */
        .header {
            background: white;
            padding: var(--space-xl);
            border-radius: var(--radius-2xl);
            box-shadow: var(--shadow-xl);
            margin-bottom: var(--space-lg);
            text-align: center;
        }

        .header h1 {
            font-size: clamp(1.75rem, 5vw, 2.5rem);
            margin-bottom: var(--space-sm);
            background: var(--primary-gradient);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            font-weight: 800;
            letter-spacing: -0.02em;
        }

        .header p {
            color: var(--gray-600);
            font-size: 1rem;
            max-width: 600px;
            margin: 0 auto;
        }

        @media (max-width: 640px) {
            .header {
                padding: var(--space-lg);
            }
        }

        .puzzle-controls {
            display: flex;
            gap: 10px;
            margin-top: 20px;
            flex-wrap: wrap;
            justify-content: center;
        }

        /* Tab Content */
        .tab-content {
            display: none;
        }

        .tab-content.active {
            display: block;
        }

        /* Main Grid */
        .main-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(450px, 1fr));
            gap: 20px;
            margin-bottom: 20px;
        }

        @media (max-width: 768px) {
            .main-grid {
                grid-template-columns: 1fr;
            }
        }

        .card {
            background: white;
            padding: 25px;
            border-radius: 15px;
            box-shadow: 0 5px 20px rgba(0,0,0,0.1);
        }

        .card h2 {
            color: #667eea;
            margin-bottom: 20px;
            font-size: 1.5em;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        /* Stats Grid */
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 15px;
            margin-top: 20px;
        }

        .stat-box {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            padding: 20px;
            border-radius: 10px;
            color: white;
            text-align: center;
        }

        .stat-label {
            font-size: 0.9em;
            opacity: 0.9;
            margin-bottom: 5px;
        }

        .stat-value {
            font-size: 2em;
            font-weight: bold;
        }

        /* Form Elements */
        .form-group {
            margin-bottom: 20px;
        }

        .form-group label {
            display: block;
            margin-bottom: 8px;
            font-weight: bold;
            color: #495057;
            font-size: 1.1em;
        }

        .form-control {
            width: 100%;
            padding: 12px;
            border: 2px solid #e9ecef;
            border-radius: 8px;
            font-size: 1em;
            transition: border-color 0.3s;
        }

        .form-control:focus {
            outline: none;
            border-color: #667eea;
        }

        .form-hint {
            font-size: 0.9em;
            color: #6c757d;
            margin-top: 5px;
        }

        /* Visual Pattern Builder */
        .pattern-builder {
            background: #f8f9fa;
            padding: 20px;
            border-radius: 10px;
            margin-top: 15px;
        }

        .pattern-parts {
            display: flex;
            flex-wrap: wrap;
            gap: 10px;
            margin-bottom: 15px;
            align-items: center;
        }

        .pattern-part {
            display: inline-flex;
            align-items: center;
            background: white;
            border: 2px solid #dee2e6;
            border-radius: 8px;
            padding: 8px 12px;
            font-family: 'Courier New', monospace;
            font-size: 1.1em;
        }

        .pattern-part.unknown {
            background: #fff3cd;
            border-color: #ffc107;
            font-weight: bold;
        }

        .pattern-part input {
            border: none;
            background: transparent;
            font-family: 'Courier New', monospace;
            font-size: 1em;
            width: 100px;
            padding: 0;
        }

        .pattern-part input:focus {
            outline: none;
        }

        .pattern-part-delete {
            margin-left: 8px;
            cursor: pointer;
            color: #dc3545;
            font-weight: bold;
        }

        .add-part-buttons {
            display: flex;
            gap: 10px;
            flex-wrap: wrap;
        }

        .pattern-preview {
            background: white;
            padding: 15px;
            border-radius: 8px;
            margin-top: 15px;
            border: 2px solid #667eea;
        }

        .pattern-preview-label {
            font-size: 0.9em;
            color: #6c757d;
            margin-bottom: 5px;
        }

        .pattern-preview-text {
            font-family: 'Courier New', monospace;
            font-size: 1.5em;
            color: #764ba2;
            font-weight: bold;
        }

        /* Options Generator */
        .options-generator {
            background: #f8f9fa;
            padding: 20px;
            border-radius: 10px;
            margin-top: 15px;
        }

        .generator-tabs {
            display: flex;
            gap: 10px;
            margin-bottom: 15px;
            border-bottom: 2px solid #dee2e6;
            padding-bottom: 10px;
        }

        .generator-tab {
            padding: 8px 16px;
            background: white;
            border: 2px solid #dee2e6;
            border-radius: 8px 8px 0 0;
            cursor: pointer;
            font-weight: bold;
            transition: all 0.3s;
        }

        .generator-tab.active {
            background: #667eea;
            color: white;
            border-color: #667eea;
        }

        .generator-content {
            display: none;
        }

        .generator-content.active {
            display: block;
        }

        .range-inputs {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 15px;
        }

        .options-preview {
            background: white;
            padding: 15px;
            border-radius: 8px;
            margin-top: 15px;
            max-height: 200px;
            overflow-y: auto;
        }

        .options-count {
            background: #667eea;
            color: white;
            padding: 8px 12px;
            border-radius: 8px;
            display: inline-block;
            margin-bottom: 10px;
            font-weight: bold;
        }

        .options-list {
            display: flex;
            flex-wrap: wrap;
            gap: 8px;
        }

        .option-tag {
            background: #e9ecef;
            padding: 5px 10px;
            border-radius: 5px;
            font-family: 'Courier New', monospace;
            font-size: 0.9em;
        }

        /* Buttons */
        .btn {
            padding: 12px 24px;
            border: none;
            border-radius: 8px;
            font-size: 1em;
            font-weight: bold;
            cursor: pointer;
            transition: all 0.3s ease;
            color: white;
        }

        .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(0,0,0,0.2);
        }

        .btn:disabled {
            opacity: 0.5;
            cursor: not-allowed;
            transform: none;
        }

        .btn-primary {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        }

        .btn-success {
            background: linear-gradient(135deg, #11998e 0%, #38ef7d 100%);
        }

        .btn-danger {
            background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
        }

        .btn-info {
            background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
        }

        .btn-warning {
            background: linear-gradient(135deg, #ffeaa7 0%, #fdcb6e 100%);
            color: #2d3436;
        }

        .btn-small {
            padding: 8px 16px;
            font-size: 0.9em;
        }

        .button-group {
            display: flex;
            gap: 10px;
            margin-top: 20px;
            flex-wrap: wrap;
        }

        /* Presets Grid */
        .presets-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
            gap: 15px;
            margin-top: 20px;
        }

        .preset-card {
            background: #f8f9fa;
            padding: 20px;
            border-radius: 10px;
            border: 2px solid transparent;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .preset-card:hover {
            border-color: #667eea;
            transform: translateY(-3px);
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
        }

        .preset-card h3 {
            color: #667eea;
            margin-bottom: 10px;
        }

        .preset-card p {
            color: #6c757d;
            font-size: 0.9em;
            margin-bottom: 10px;
        }

        .preset-pattern {
            font-family: 'Courier New', monospace;
            background: white;
            padding: 8px;
            border-radius: 5px;
            font-size: 0.9em;
            margin-top: 10px;
            color: #764ba2;
            font-weight: bold;
        }

        /* Recommended Numbers */
        .recommended-numbers {
            margin-top: 15px;
        }

        .recommended-item {
            background: linear-gradient(135deg, #ffeaa7 0%, #fdcb6e 100%);
            padding: 15px;
            margin-bottom: 10px;
            border-radius: 8px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            font-weight: bold;
            color: #2d3436;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .recommended-item:hover {
            transform: translateX(5px);
            box-shadow: 0 3px 10px rgba(0,0,0,0.2);
        }

        .priority-badge {
            background: #d63031;
            color: white;
            padding: 5px 10px;
            border-radius: 20px;
            font-size: 0.8em;
        }

        /* Phone Grid */
        .phone-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
            gap: 10px;
            margin-top: 20px;
        }

        .phone-item {
            background: #f8f9fa;
            padding: 15px;
            border-radius: 8px;
            border: 2px solid transparent;
            cursor: pointer;
            transition: all 0.3s ease;
            text-align: center;
        }

        .phone-item:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
        }

        .phone-item.checked {
            background: #ffebee;
            border-color: #f44336;
            opacity: 0.6;
        }

        .phone-item.unchecked {
            background: #e8f5e9;
            border-color: #4caf50;
        }

        .phone-number {
            font-weight: bold;
            font-size: 1.1em;
            margin-bottom: 5px;
            color: #333;
        }

        .phone-option {
            font-size: 0.9em;
            color: #666;
        }

        .phone-status {
            margin-top: 8px;
            font-size: 0.85em;
            font-weight: bold;
        }

        .phone-item.checked .phone-status {
            color: #f44336;
        }

        .phone-item.unchecked .phone-status {
            color: #4caf50;
        }

        /* Progress Bar */
        .progress-bar {
            background: #e0e0e0;
            border-radius: 10px;
            height: 30px;
            margin-top: 20px;
            overflow: hidden;
        }

        .progress-fill {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            height: 100%;
            transition: width 0.5s ease;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-weight: bold;
        }

        /* Alert */
        .alert {
            padding: 15px;
            border-radius: 8px;
            margin-top: 15px;
            font-weight: bold;
        }

        .alert-success {
            background: #d4edda;
            color: #155724;
            border: 1px solid #c3e6cb;
        }

        .alert-info {
            background: #d1ecf1;
            color: #0c5460;
            border: 1px solid #bee5eb;
        }

        .alert-warning {
            background: #fff3cd;
            color: #856404;
            border: 1px solid #ffeeba;
        }

        /* Saved Puzzles List */
        .saved-puzzle-item {
            background: #f8f9fa;
            padding: 15px;
            border-radius: 8px;
            margin-bottom: 10px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .saved-puzzle-info h4 {
            color: #667eea;
            margin-bottom: 5px;
        }

        .saved-puzzle-info p {
            color: #6c757d;
            font-size: 0.9em;
        }

        .saved-puzzle-actions {
            display: flex;
            gap: 8px;
        }

        /* Loading */
        .loading {
            text-align: center;
            padding: 20px;
            color: #667eea;
        }

        .full-width {
            grid-column: 1 / -1;
        }

        /* Step indicator */
        .step-indicator {
            display: flex;
            justify-content: center;
            gap: 15px;
            margin-bottom: 30px;
            padding: 20px;
            background: #f8f9fa;
            border-radius: 10px;
        }

        .step {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 8px;
        }

        .step-number {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background: #dee2e6;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: bold;
            color: #6c757d;
        }

        .step.active .step-number {
            background: #667eea;
            color: white;
        }

        .step.completed .step-number {
            background: #28a745;
            color: white;
        }

        .step-label {
            font-size: 0.9em;
            color: #6c757d;
        }

        .step.active .step-label {
            color: #667eea;
            font-weight: bold;
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Header -->
        <div class="header">
            <h1>📱 Universal Phone Puzzle Solver</h1>
            <p style="font-size: 1.2em; color: #666; margin-top: 10px;">
                Create custom puzzles or use presets. Find missing digits with optimal probability strategy.
            </p>

            <div class="puzzle-controls">
                <button class="btn btn-primary" onclick="showTab('solve')">🎯 Solve Puzzle</button>
                <button class="btn btn-success" onclick="showTab('create')">➕ Create Custom</button>
                <button class="btn btn-info" onclick="showTab('presets')">📋 Load Preset</button>
                <button class="btn btn-warning" onclick="showTab('saved')">💾 My Puzzles</button>
            </div>

            <!-- Current Puzzle Display -->
            <div class="alert alert-info" style="margin-top: 20px;">
                <strong>Current Puzzle:</strong> <span id="currentPuzzleName">Loading...</span><br>
                <strong>Pattern:</strong> <span id="currentPuzzlePattern" style="font-family: 'Courier New', monospace;">Loading...</span>
            </div>

            <!-- Progress Bar -->
            <div class="progress-bar">
                <div class="progress-fill" id="progressBar" style="width: 0%">0% Checked</div>
            </div>
        </div>

        <!-- Tab: Solve Puzzle -->
        <div id="tabSolve" class="tab-content active">
            <div class="main-grid">
                <!-- Statistics Card -->
                <div class="card">
                    <h2><span>📊</span> Probability Analysis</h2>

                    <div class="stats-grid">
                        <div class="stat-box">
                            <div class="stat-label">Total Options</div>
                            <div class="stat-value" id="totalCount">0</div>
                        </div>
                        <div class="stat-box">
                            <div class="stat-label">Remaining</div>
                            <div class="stat-value" id="remainingCount">0</div>
                        </div>
                        <div class="stat-box">
                            <div class="stat-label">Next Try</div>
                            <div class="stat-value" id="probNext">0%</div>
                        </div>
                        <div class="stat-box">
                            <div class="stat-label">Within 5 Tries</div>
                            <div class="stat-value" id="probWithin5">0%</div>
                        </div>
                    </div>

                    <div class="alert alert-info" style="margin-top: 20px;">
                        <strong>Expected Attempts:</strong> <span id="expectedAttempts">0</span> tries (average)
                    </div>

                    <div class="button-group">
                        <button class="btn btn-danger" onclick="resetAll()">🔄 Reset Progress</button>
                        <button class="btn btn-info" onclick="exportData()">💾 Export Results</button>
                    </div>
                </div>

                <!-- Strategy Card -->
                <div class="card">
                    <h2><span>🎯</span> Optimal Strategy</h2>

                    <div class="alert alert-success">
                        Try these numbers first for highest efficiency!
                    </div>

                    <div class="recommended-numbers" id="recommendedNumbers">
                        <div class="loading">Loading strategy...</div>
                    </div>

                    <div class="button-group">
                        <button class="btn btn-primary" onclick="loadStrategy()">🔄 Refresh Strategy</button>
                        <button class="btn btn-success" onclick="toggleAllCombinations()">📋 Show All Numbers</button>
                    </div>
                </div>
            </div>

            <!-- All Combinations Card -->
            <div class="card full-width" id="combinationsCard" style="display: none;">
                <h2><span>📞</span> All Possible Numbers</h2>
                <p style="color: #666; margin-bottom: 15px;">Click any number to mark as checked/unchecked</p>

                <div class="phone-grid" id="phoneGrid">
                    <div class="loading">Loading combinations...</div>
                </div>
            </div>
        </div>

        <!-- Tab: Create Custom Puzzle -->
        <div id="tabCreate" class="tab-content">
            <div class="card">
                <h2><span>➕</span> Create Your Puzzle - Simple & Easy!</h2>

                <!-- Step Indicator -->
                <div class="step-indicator">
                    <div class="step active" id="step1">
                        <div class="step-number">1</div>
                        <div class="step-label">Name</div>
                    </div>
                    <div class="step" id="step2">
                        <div class="step-number">2</div>
                        <div class="step-label">Build Pattern</div>
                    </div>
                    <div class="step" id="step3">
                        <div class="step-number">3</div>
                        <div class="step-label">Add Options</div>
                    </div>
                </div>

                <form id="createPuzzleForm" onsubmit="createSimplePuzzle(event)">
                    <!-- Step 1: Name -->
                    <div class="form-group">
                        <label>📝 Give your puzzle a name</label>
                        <input type="text" class="form-control" id="puzzleName" required
                               placeholder="e.g., Find My Friend's Number">
                        <div class="form-hint">This helps you remember what this puzzle is for</div>
                    </div>

                    <!-- Step 2: Pattern Builder -->
                    <div class="form-group">
                        <label>🎨 Build Your Phone Number Pattern</label>
                        <div class="form-hint" style="margin-bottom: 15px;">
                            Click buttons below to build your pattern piece by piece
                        </div>

                        <div class="pattern-builder">
                            <div class="pattern-parts" id="patternParts">
                                <div class="pattern-part" style="opacity: 0.5;">
                                    Start building your pattern →
                                </div>
                            </div>

                            <div class="add-part-buttons">
                                <button type="button" class="btn btn-primary btn-small" onclick="addPatternPart('text')">
                                    ➕ Add Text (e.g., +998)
                                </button>
                                <button type="button" class="btn btn-warning btn-small" onclick="addPatternPart('unknown')">
                                    ❓ Add Unknown Part
                                </button>
                                <button type="button" class="btn btn-danger btn-small" onclick="clearPattern()">
                                    🗑️ Clear All
                                </button>
                            </div>

                            <div class="pattern-preview">
                                <div class="pattern-preview-label">Preview:</div>
                                <div class="pattern-preview-text" id="patternPreview">-</div>
                            </div>
                        </div>
                    </div>

                    <!-- Step 3: Options Generator -->
                    <div class="form-group">
                        <label>🔢 What are the possible values for the unknown part?</label>
                        <div class="form-hint" style="margin-bottom: 15px;">
                            Choose how to generate the list of options to try
                        </div>

                        <div class="options-generator">
                            <div class="generator-tabs">
                                <div class="generator-tab active" onclick="showGenerator('range')">
                                    📊 Number Range
                                </div>
                                <div class="generator-tab" onclick="showGenerator('list')">
                                    📝 Custom List
                                </div>
                                <div class="generator-tab" onclick="showGenerator('upload')">
                                    📄 Paste List
                                </div>
                            </div>

                            <!-- Range Generator -->
                            <div class="generator-content active" id="genRange">
                                <div class="range-inputs">
                                    <div>
                                        <label>From:</label>
                                        <input type="number" class="form-control" id="rangeFrom"
                                               value="100" onchange="generateRangeOptions()">
                                    </div>
                                    <div>
                                        <label>To:</label>
                                        <input type="number" class="form-control" id="rangeTo"
                                               value="200" onchange="generateRangeOptions()">
                                    </div>
                                </div>
                                <div style="margin-top: 15px;">
                                    <label>
                                        <input type="checkbox" id="rangePad" onchange="generateRangeOptions()">
                                        Pad with zeros (e.g., 001, 002, 003)
                                    </label>
                                </div>
                            </div>

                            <!-- List Generator -->
                            <div class="generator-content" id="genList">
                                <div class="form-hint">Enter each value separated by comma</div>
                                <input type="text" class="form-control" id="listValues"
                                       placeholder="e.g., 010, 112, 113, 342, 345"
                                       onchange="generateListOptions()">
                            </div>

                            <!-- Upload Generator -->
                            <div class="generator-content" id="genUpload">
                                <div class="form-hint">Paste your list (one per line)</div>
                                <textarea class="form-control" id="uploadValues"
                                          rows="8" placeholder="010&#10;112&#10;113&#10;342&#10;345"
                                          onchange="generateUploadOptions()"></textarea>
                            </div>

                            <!-- Options Preview -->
                            <div class="options-preview">
                                <div class="options-count" id="optionsCount">0 options</div>
                                <div class="options-list" id="optionsPreview">
                                    <div style="color: #6c757d;">Options will appear here...</div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Submit -->
                    <div class="button-group">
                        <button type="submit" class="btn btn-success" id="createBtn">
                            ✅ Create Puzzle & Start Solving
                        </button>
                        <button type="button" class="btn btn-info" onclick="showQuickExample()">
                            💡 Show Me an Example
                        </button>
                    </div>
                </form>
            </div>
        </div>

        <!-- Tab: Presets -->
        <div id="tabPresets" class="tab-content">
            <div class="card">
                <h2><span>📋</span> Preset Puzzles</h2>
                <p style="color: #666; margin-bottom: 15px;">Choose from pre-configured puzzle templates</p>

                <div class="presets-grid" id="presetsGrid">
                    <div class="loading">Loading presets...</div>
                </div>
            </div>
        </div>

        <!-- Tab: Saved Puzzles -->
        <div id="tabSaved" class="tab-content">
            <div class="card">
                <h2><span>💾</span> My Saved Puzzles</h2>
                <p style="color: #666; margin-bottom: 15px;">Your custom puzzles are saved automatically</p>

                <div id="savedPuzzlesList">
                    <div class="loading">Loading saved puzzles...</div>
                </div>
            </div>
        </div>
    </div>

    <script src="{{ url_for('static', filename='app.js') }}"></script>
</body>
</html>
